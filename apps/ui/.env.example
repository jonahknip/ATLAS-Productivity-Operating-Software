# ATLAS UI Configuration
# Copy this to .env.local and fill in your values

# =============================================================================
# API Connection
# =============================================================================

# API URL - leave empty for local development (uses Vite proxy)
# For production: set to your Railway API URL
# Example: https://atlas-api-production.up.railway.app
VITE_ATLAS_API_URL=

# API Token - required when connecting to production API
# Must match the API_TOKEN set on the API server
VITE_ATLAS_API_TOKEN=
